<div class="col col-md-10">
  <h1><%= t('.header') %></h1>

  <table class="table">
    <thead class="thead-dark">
      <tr>
        <th><%= t('.title') %></th>
        <th><%= t('.level') %></th>
        <th><%= t('.amount_question') %></th>
        <th><%= t('.action') %></th>
      </tr>
    </thead>
    <tbody>
      <%= render @tests%>
    </tbody>
  </table>
</div>
