<div class="col col-md-10">
  <h1><%= t('.header') %></h1>

  <table class="table">
    <thead class="thead-dark">
      <tr>
        <th class="sort-by-title pointer">
          <%= t('.title') %>
          <%= octicon 'arrow-up', class: 'text-success hide' %>
          <%= octicon 'arrow-down', class: 'text-success hide' %>
        </th>
        <th><%= t('.level') %></th>
        <th><%= t('.amount_question') %></th>
        <th><%= t('.action') %></th>
      </tr>
    </thead>
    <tbody>
      <%= render @tests%>
    </tbody>
  </table>
</div>
